[FIXED]
* Fixed an issue where customization color settings and toggles would not update the UI immediately without restarting or changing tabs.
* Fixed CI build scripts generating incorrect version codes (version 1), which prevented local builds from successfully upgrading over CI builds.
